# ── Dependências ──
node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# ── Nx ──
.nx/cache
.nx/workspace-data
migrations.json

# ── Builds / saída ──
dist/
tmp/
out-tsc/
build/
*.tsbuildinfo

# ── Angular ──
.angular/
.angular/cache/

# ── Testes / cobertura ──
coverage/
test-output/
*.lcov
.vitest/
.jest/

# ── Ambiente / segredos ──
.env
.env.*
!.env.example
*.local
secrets/
*.pem
*.key

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
report.*.json

# ── Editores / IDE ──
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
.idea/
*.swp
*.swo
*~

# ── Sistema operacional ──
.DS_Store
Thumbs.db

# ── Diretórios temporários de agentes / scratchpad ──
.scratchpad/
scratchpad/

# NOTA: _bmad-output/ é versionado de propósito — é o contrato do projeto
# (PRD, UX, arquitetura, SPEC, épicos e stories). Não ignorar.

.angular
