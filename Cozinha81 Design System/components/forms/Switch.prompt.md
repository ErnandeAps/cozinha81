Binary toggle for settings (e.g. "Aceitar reservas automáticas"). Flame track when on.

```jsx
<Switch label="Reservas automáticas" defaultChecked />
<Switch checked={open} onChange={setOpen} label="Cozinha aberta" />
```
