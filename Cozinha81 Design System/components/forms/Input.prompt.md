Text field with optional label and hint. Bare (no label/hint) it renders just the control for custom layouts.

```jsx
<Input label="Nome da operação" placeholder="Ex.: Burger do Zé" />
<Input label="Observações" as="textarea" hint="Opcional" />
<Input invalid placeholder="CNPJ inválido" />
```
