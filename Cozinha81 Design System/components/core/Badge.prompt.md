Compact uppercase-mono label for metadata, categories, and counts. For live kitchen state prefer `StatusPill`.

```jsx
<Badge variant="accent">24h</Badge>
<Badge variant="ready" dot>Disponível</Badge>
<Badge variant="solid">NOVO</Badge>
```

Variants: `neutral | accent | ready | warn | stop | solid`. `dot` adds a leading indicator.
