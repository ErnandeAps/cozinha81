User/brand avatar. Pass `name` for auto initials, or `src` for a photo.

```jsx
<Avatar name="Marina Tavares" />
<Avatar src="/chef.jpg" name="Chef" size="lg" />
```

Sizes: `sm | md | lg`. `accent` fills flame instead of ink.
