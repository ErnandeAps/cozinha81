Live availability indicator for a kitchen/station. The signature Cozinha81 component — `ready` animates a pulsing ring (the lit burner).

```jsx
<StatusPill status="ready" />        {/* Disponível, pulsing */}
<StatusPill status="warn" />         {/* Em preparo */}
<StatusPill status="stop" label="Reservada" />
```
